export function setBackground(color: string): { backgroundColor: string } {
  return { backgroundColor: `#${color}` };
}

export function setColor(color: string): { color: string } {
  return { color: `#${color}` };
}
